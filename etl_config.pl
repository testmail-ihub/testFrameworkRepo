#!/usr/bin/perl

use strict;
use warnings;

# Database configuration
our $DB_HOST = 'localhost';
our $DB_NAME = 'source_db';
our $DB_USER = 'your_username';
our $DB_PASS = 'your_password';

# File paths
our $RAW_DATA_CSV = 'raw_data.csv';
our $CLEANED_DATA_CSV = 'cleaned_data.csv';

1;